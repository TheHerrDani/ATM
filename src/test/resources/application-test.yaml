spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL57InnoDBDialect
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/zinkworks
    username: ZINKWORKS
    password: ZINKWORKS
    driver-class-name: com.mysql.cj.jdbc.Driver
  liquibase:
    enabled: true
logging:
  level:
    org:
      springframework:
        web: TRACE
        security: TRACE